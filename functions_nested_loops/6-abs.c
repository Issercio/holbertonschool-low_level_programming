#include "main.h"
#include <stdio.h>

/**
 * _abs - Check that function that computes the absolute value
 * @n: the integer to compute the absolute value of
 *
 * Return: the absolute value of n
 */
int _abs(int n)
{
if (n < 0)
return (-n);
return (n);
}
